name: Build and Deploy
on:
  push:
    branches: [ master ]
permissions:
  contents: write
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install and Build 🔧
        run: |
          # 1. Install specific legacy compatibility layers
          npm install react-is --legacy-peer-deps
          
          # 2. Install the Icon library needed for the Sun/Moon toggle
          npm install react-icons --legacy-peer-deps
          
          # 3. Install all other project dependencies
          npm install --legacy-peer-deps
          
          # 4. Generate the optimized production build
          npm run build
        env:
          CI: false

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: gh-pages
          folder: build
